# Git hooks are run by Git-for-Windows' /bin/sh, which chokes on CRLF (the `\r` becomes part of the
# shebang/commands). Keep hooks and shell scripts LF regardless of core.autocrlf.
scripts/githooks/** text eol=lf
*.sh text eol=lf
